PHP classes for ARP Restaurant belong in this directory under the `Anatolkin\ArpRestaurant` namespace.

Backend lifecycle and the compact editor live under `Backend/`. Domain models are not shipped yet.
